stages:
  - build
  - test

cache:
  key: node_modules
  paths:
    - .npm/

before_script:
  - npm ci --cache .npm --prefer-offline
  
build:
  stage: build
  script:
    - npm install

test:
  stage: test
  script:
    - npm run test
